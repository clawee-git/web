{
  "_comment": "Docs-sync marker for clawee.org. `surfaces` is the published UI/command surface: every place in the code whose text a user reads off this site. Each has a matching `DOCS-SYNC <anchor>` comment at that spot in the source — `grep -rn 'DOCS-SYNC' <repo>` is the authoritative list, and it should agree with this one. `syncs` records what each pass was verified against, so the next pass reviews `git log <sha>..origin/main` per repo instead of re-auditing. Never edit a past sync entry — prepend a new one.",

  "surfaces": [
    {
      "view": "top-level help",
      "src": "cli:cmd/clawee/usage.go buildUsage (generated from help.go helpTree)",
      "anchor": "/docs#commands",
      "note": "buildUsage now renders the FULL command tree (every sub-verb, every flag — operator ruling 2026-08-21), not just the first level; it walks the same helpTree that cli:cmd/clawee/docs.go renderDocs walks. The GENERATED cli:docs/cli-help.md (`clawee docs`, build-gated by TestDocsFileIsInSync) additionally fences each verb's verbatim --help page beneath the tree; sync command tables from that file, never from this page alone."
    },
    {
      "view": "relays verbs",
      "src": "cli:cmd/clawee/docs.go renderDocs (#relays marker; auto/gateway appear only at this level)",
      "anchor": "/docs#relays",
      "note": "The full verb set (auto/gateway included). The hand-written relaysUsage string is GONE (2026-08-20 cli help port); `clawee relays --help` and the generated reference are the surface."
    },
    {
      "view": "top-level Commands table",
      "src": "cli:cmd/clawee/docs.go:46 renderDocs",
      "anchor": "/docs#commands",
      "note": "top-level Commands table publishes from the generated reference (cli:docs/cli-help.md via `clawee docs`), byte-identical to the binary's own --help by construction."
    },
    {
      "view": "session picker",
      "src": "cli:internal/picker/render.go tabBar",
      "anchor": "/docs#keys",
      "note": "Per-tab: the Local and Remote hints differ (d = devices vs detach)."
    },
    {
      "view": "pairing form",
      "src": "cli:internal/picker/pair_header.go pairFormHint",
      "anchor": "/docs#pairing",
      "note": "Five states now (PairFormPair / PairFormWait / PairFormContinue / PairFormUser / PairFormNoUser): pair / re-check / continue / set-user-with-recheck / set-user-without-recheck. PairFormNoUser drops `r` deliberately — a re-check with no tenant selected is a hello claiming nobody, which the gateway can only refuse."
    },
    {
      "view": "gateway chooser",
      "src": "cli:cmd/clawee/gateway_modal.go gwHint",
      "anchor": "/docs#gateways",
      "note": "Three bars: gwHint (in-picker, 'q back'), gwHintQuit (entry screen, countdown not armed, 'q quit'), gwHintEntryMode (entry screen, countdown armed — drops 'u user' to hold the 80-column budget; `u` still works while armed, just isn't listed)."
    },
    {
      "view": "relay list — bar, ⏎ verbs, columns",
      "src": "cli:internal/relay_pref/list_render.go RenderList",
      "anchor": "/docs#relays"
    },
    {
      "view": "relay list — key handler",
      "src": "cli:cmd/clawee/relays_ui.go handleKey",
      "anchor": "/docs#relays",
      "note": "Accepts MORE than the bar shows: +/- alias K/J, k/j alias arrows, q closes. The bar cannot grow (80-col budget), so the docs page is the only place these are published. 'G' (cycle gateway) was REMOVED here on 2026-07-29 — do not re-document it."
    },
    {
      "view": "relay quick switch (picker `l`)",
      "src": "cli:cmd/clawee/relay_quickswitch.go quickRelayLines",
      "anchor": "/docs#relays"
    },
    {
      "view": "devices view",
      "src": "cli:cmd/clawee/devices_view.go devicesHint",
      "anchor": "/docs#devices",
      "note": "Acts on trust (approve/revoke a device key) — a silent drift here is worse than cosmetic."
    },
    {
      "view": "lock screen",
      "src": "cli:internal/lock_screen/render.go Render",
      "anchor": "/docs#lock",
      "note": "Advertises only 'type the passphrase + Enter'."
    },
    {
      "view": "lock screen — editing keys",
      "src": "cli:internal/lock_input/lock_input.go",
      "anchor": "/docs#lock",
      "note": "Ctrl+U reveal / Ctrl+Backspace clear appear NOWHERE on screen. Docs are their only home."
    },
    {
      "view": "/history pager",
      "src": "cli:cmd/clawee/history_pager_ui.go pageAltScreen",
      "anchor": "/docs#slash",
      "note": "No key bar is rendered at all — every key is invisible until pressed."
    }
  ],

  "syncs": [
    {
      "date": "2026-08-21",
      "scope": "released v0.2.x catch-up: help port (help verb removed, docs verb, level-by-level --help), claweed setup + tenant add, chooser tree re-layout, interrupted sessions, unlock/refresh repaint, one-way upgrade callout; published /docs/clawee + /docs/claweed generated references",
      "released": {
        "clawee": "v0.2.12",
        "claweed": "v0.2.4"
      },
      "repos": {
        "clawee-git/cli": "c995a53",
        "clawee-git/daemon": "64a2dfc"
      },
      "notes": "This pass re-verifies the 2026-08-20 dev-synced entry (5ddcaa9/f64b907) against the RELEASED surface (c995a53/64a2dfc) and bumps the 'Docs synced' stamp that entry deliberately withheld — the multi-user surface it documented has now shipped. Also new this pass: the two published generated-reference pages /docs/clawee + /docs/claweed (HTML + raw cli-help.md copies, byte-identical to cli c995a53 / daemon 64a2dfc). Verdicts recorded: (1) relay-list `G` key — confirmed REMOVED in released code (cmd/clawee/relays_ui.go, no live handler, explicit 'do not re-add' comment beneath the switch); however the DOCS-SYNC comment ABOVE that same switch is stale and still lists 'G switches gateway' — filed as a cli-repo comment bug, not a docs bug; the page correctly stays q/Esc→chooser and does not re-document G. (2) `-r` attach flag — absent from the released generated reference's flag list; removed from #attach and from the home page's resumable-session card, which had mentioned `clawee -r`. (3) no-user pairing hint — the released binary has TWO DISTINCT string defects, neither of which the site repeats: a WORD-ORDER bug at cmd/clawee/main.go:407 and cmd/clawee/doctor.go:317, which print `clawee gateway user add <fp|name> <user>` (`add` present, but before `<fp|name>` instead of after it); and a MISSING-`add` bug at cmd/clawee/pair_view.go:317, pair_view.go:339, and cmd/clawee/status.go:286, which print `clawee gateway user <fp|name> <user>` (no `add` token at all). The actual verb requires the canonical `clawee gateway user <fp|name> add <user>`, which is the only form the site prints; recorded as documented-correctly-on-site, not a docs defect."
    },
    {
      "date": "2026-08-20",
      "scope": "gateway multi-user: tree picker (gateway + user rows), per-user devices view (pending-first, `devices · <gw> · <user>` title), devices approve by bare/index/fingerprint, and the pairing form's new no-user/user-refused states",
      "released": {
        "clawee": "unreleased (dev)",
        "claweed": "unreleased (dev)"
      },
      "repos": {
        "clawee-git/cli": "5ddcaa9",
        "clawee-git/daemon": "f64b907"
      },
      "notes": "Synced against the DEV WORKTREES, not a released tag — the multi-user plan (docs/specs/2026-08-20-clawee-gateway-multi-user-design.md, resources repo) had not been cut at sync time, per the plan's own Task 13. Deliberately NOT against 'main' either: this documents what will ship once released. Updated #commands (devices/gateway top-level lines), #gateways (tree model: use <fp|name> [<user>], user <fp|name> add|rm <user>, GATEWAY list gains a USERS column, chooser keys r/u split — u now ADDS a user rather than overwriting one), #devices (per-(gateway,user) trust, pending-first queue, devices · <gw> · <user> title, approve [<#>|<fp>] semantics), and #pairing (new PairFormNoUser 'no user set' / 'user set — <user>' bar and PairFormUser 'user refused by the gateway — <user>' bar, plus the approve syntax). docs/index.html and llms-full.txt both updated (kept in lockstep per this repo's CLAUDE.md); the 'Docs synced' footer stamp is UNCHANGED — it names a released version, and none has shipped yet. Re-verify (and bump the stamp) once clawee/claweed actually release with this surface."
    },
    {
      "date": "2026-07-29",
      "scope": "stamp only — no page content changed",
      "released": {
        "clawee": "v0.1.102",
        "claweed": "v0.1.42"
      },
      "repos": {
        "clawee-git/cli": "04e3b4c",
        "clawee-git/daemon": "c5ad76d"
      },
      "notes": "Both components were re-cut to ship installer fixes, so the stamp had to move — but NOTHING on the page changed, and the review that justifies saying so is recorded here. cli is the SAME commit as the previous entry (04e3b4c): clawee v0.1.102's binary is byte-identical to v0.1.101 and only the inner installer inside the zip differs. The daemon delta d186508..c5ad76d touches install/install.sh.in and install_test.sh only — the installer's own output, which this site does not document (it documents the install one-liner, not what the installer prints). Version-parse fixes: release 8611a21 (clawee installer) + daemon #75 (claweed installer, both callers)."
    },
    {
      "date": "2026-07-29",
      "scope": "release catch-up: stamp refresh + doctor version reporting",
      "released": {
        "clawee": "v0.1.101",
        "claweed": "v0.1.41"
      },
      "repos": {
        "clawee-git/cli": "04e3b4c",
        "clawee-git/daemon": "d186508"
      },
      "notes": "clawee v0.1.101 + claweed v0.1.41 shipped, which CLOSES the divergence the previous entry recorded: the relay list's 'G' removal is now in released binaries, so the page describing q/Esc → chooser is true of what users run. Also documents what those releases added — `clawee doctor` now reports versions on every run including a dead chain (it used to print them only on the success path), and `claweed doctor` gained an installed-vs-running version row. Footer stamp moved v0.1.99/v0.1.39 → v0.1.101/v0.1.41 in both docs/index.html and llms-full.txt. A separate entry rather than an edit of the one below: that entry is an accurate record of what was verified at the time, divergence included."
    },
    {
      "date": "2026-07-29",
      "scope": "full alternate-view sweep: every picker/chooser/list/lock/pager view, its keys and description; DOCS-SYNC markers added in cli",
      "released": {
        "clawee": "v0.1.99",
        "claweed": "v0.1.39"
      },
      "repos": {
        "clawee-git/cli": "1276dcd",
        "clawee-git/daemon": "2a7cb15"
      },
      "notes": "Second pass the same day, after the gateway/relay pass below. Added Views & keys index, Devices and Locking sections, the relay list's unadvertised keys (+/-, q/Esc), the Remote tab's differing row actions, and the /history pager keys. Every surface above now carries a DOCS-SYNC comment in cli (branch docs-sync-markers, off 1780887). Documenting the surface also RETIRED one key: the relay list's 'G' cycled gateway on a single unconfirmed keystroke — removed in the same branch (handler, hint, cycleGateway helper, tests), so the docs describe q/Esc → chooser instead. This sync therefore documents cli main + that branch, NOT released v0.1.99: until the branch ships, the released binary still answers G."
    },
    {
      "date": "2026-07-29",
      "scope": "gateway + relay surface, picker keys, in-picker device pairing",
      "released": {
        "clawee": "v0.1.99",
        "claweed": "v0.1.39"
      },
      "repos": {
        "clawee-git/cli": "1276dcd",
        "clawee-git/daemon": "2a7cb15"
      },
      "notes": "Synced against the RELEASED surface, not cli main. cli 3835270 (#159 connection events) and 1780887 (#160 closed-session replay contract) were on main but unreleased at sync time and carry no doc-visible surface. daemon commits after 2a7cb15 (conn_log/audit) are internal."
    },
    {
      "date": "2026-07-17",
      "scope": "canonical `gateway` verb (gw = alias)",
      "released": {
        "clawee": "v0.1.92"
      },
      "repos": {
        "clawee-git/cli": "d253337"
      },
      "notes": "Reconstructed from web commit 413e5b9. docs/index.html only — llms-full.txt was missed and still carried `gw` as canonical until the 2026-07-29 sync."
    }
  ]
}
