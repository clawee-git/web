{
  "_comment": "Docs-sync marker for clawee.org. `surfaces` is the published UI/command surface: every place in the code whose text a user reads off this site. Each has a matching `DOCS-SYNC <anchor>` comment at that spot in the source — `grep -rn 'DOCS-SYNC' <repo>` is the authoritative list, and it should agree with this one. `syncs` records what each pass was verified against, so the next pass reviews `git log <sha>..origin/main` per repo instead of re-auditing. Never edit a past sync entry — prepend a new one.",

  "surfaces": [
    {
      "view": "top-level help",
      "src": "cli:cmd/clawee/usage.go printUsage",
      "anchor": "/docs#commands",
      "note": "TOP-LEVEL map only — several verbs accept more than it lists. Never sync the Commands table from this file alone."
    },
    {
      "view": "relays verbs",
      "src": "cli:cmd/clawee/relays_cmd.go relaysUsage",
      "anchor": "/docs#relays",
      "note": "The full verb set. `auto` and `gateway` appear ONLY here, not in printUsage."
    },
    {
      "view": "session picker",
      "src": "cli:internal/picker/render.go tabBar",
      "anchor": "/docs#keys",
      "note": "Per-tab: the Local and Remote hints differ (d = devices vs detach)."
    },
    {
      "view": "pairing form",
      "src": "cli:internal/picker/pair_header.go pairFormHint",
      "anchor": "/docs#pairing",
      "note": "One bar per state: pair / re-check / continue."
    },
    {
      "view": "gateway chooser",
      "src": "cli:cmd/clawee/gateway_modal.go gwHint",
      "anchor": "/docs#gateways"
    },
    {
      "view": "relay list — bar, ⏎ verbs, columns",
      "src": "cli:internal/relay_pref/list_render.go RenderList",
      "anchor": "/docs#relays"
    },
    {
      "view": "relay list — key handler",
      "src": "cli:cmd/clawee/relays_ui.go handleKey",
      "anchor": "/docs#relays",
      "note": "Accepts MORE than the bar shows: +/- alias K/J, k/j alias arrows, q closes. The bar cannot grow (80-col budget), so the docs page is the only place these are published. 'G' (cycle gateway) was REMOVED here on 2026-07-29 — do not re-document it."
    },
    {
      "view": "relay quick switch (picker `l`)",
      "src": "cli:cmd/clawee/relay_quickswitch.go quickRelayLines",
      "anchor": "/docs#relays"
    },
    {
      "view": "devices view",
      "src": "cli:cmd/clawee/devices_view.go devicesHint",
      "anchor": "/docs#devices",
      "note": "Acts on trust (approve/revoke a device key) — a silent drift here is worse than cosmetic."
    },
    {
      "view": "lock screen",
      "src": "cli:internal/lock_screen/render.go Render",
      "anchor": "/docs#lock",
      "note": "Advertises only 'type the passphrase + Enter'."
    },
    {
      "view": "lock screen — editing keys",
      "src": "cli:internal/lock_input/lock_input.go",
      "anchor": "/docs#lock",
      "note": "Ctrl+U reveal / Ctrl+Backspace clear appear NOWHERE on screen. Docs are their only home."
    },
    {
      "view": "/history pager",
      "src": "cli:cmd/clawee/history_pager_ui.go pageAltScreen",
      "anchor": "/docs#slash",
      "note": "No key bar is rendered at all — every key is invisible until pressed."
    }
  ],

  "syncs": [
    {
      "date": "2026-07-29",
      "scope": "stamp only — no page content changed",
      "released": {
        "clawee": "v0.1.102",
        "claweed": "v0.1.42"
      },
      "repos": {
        "clawee-git/cli": "04e3b4c",
        "clawee-git/daemon": "c5ad76d"
      },
      "notes": "Both components were re-cut to ship installer fixes, so the stamp had to move — but NOTHING on the page changed, and the review that justifies saying so is recorded here. cli is the SAME commit as the previous entry (04e3b4c): clawee v0.1.102's binary is byte-identical to v0.1.101 and only the inner installer inside the zip differs. The daemon delta d186508..c5ad76d touches install/install.sh.in and install_test.sh only — the installer's own output, which this site does not document (it documents the install one-liner, not what the installer prints). Version-parse fixes: release 8611a21 (clawee installer) + daemon #75 (claweed installer, both callers)."
    },
    {
      "date": "2026-07-29",
      "scope": "release catch-up: stamp refresh + doctor version reporting",
      "released": {
        "clawee": "v0.1.101",
        "claweed": "v0.1.41"
      },
      "repos": {
        "clawee-git/cli": "04e3b4c",
        "clawee-git/daemon": "d186508"
      },
      "notes": "clawee v0.1.101 + claweed v0.1.41 shipped, which CLOSES the divergence the previous entry recorded: the relay list's 'G' removal is now in released binaries, so the page describing q/Esc → chooser is true of what users run. Also documents what those releases added — `clawee doctor` now reports versions on every run including a dead chain (it used to print them only on the success path), and `claweed doctor` gained an installed-vs-running version row. Footer stamp moved v0.1.99/v0.1.39 → v0.1.101/v0.1.41 in both docs/index.html and llms-full.txt. A separate entry rather than an edit of the one below: that entry is an accurate record of what was verified at the time, divergence included."
    },
    {
      "date": "2026-07-29",
      "scope": "full alternate-view sweep: every picker/chooser/list/lock/pager view, its keys and description; DOCS-SYNC markers added in cli",
      "released": {
        "clawee": "v0.1.99",
        "claweed": "v0.1.39"
      },
      "repos": {
        "clawee-git/cli": "1276dcd",
        "clawee-git/daemon": "2a7cb15"
      },
      "notes": "Second pass the same day, after the gateway/relay pass below. Added Views & keys index, Devices and Locking sections, the relay list's unadvertised keys (+/-, q/Esc), the Remote tab's differing row actions, and the /history pager keys. Every surface above now carries a DOCS-SYNC comment in cli (branch docs-sync-markers, off 1780887). Documenting the surface also RETIRED one key: the relay list's 'G' cycled gateway on a single unconfirmed keystroke — removed in the same branch (handler, hint, cycleGateway helper, tests), so the docs describe q/Esc → chooser instead. This sync therefore documents cli main + that branch, NOT released v0.1.99: until the branch ships, the released binary still answers G."
    },
    {
      "date": "2026-07-29",
      "scope": "gateway + relay surface, picker keys, in-picker device pairing",
      "released": {
        "clawee": "v0.1.99",
        "claweed": "v0.1.39"
      },
      "repos": {
        "clawee-git/cli": "1276dcd",
        "clawee-git/daemon": "2a7cb15"
      },
      "notes": "Synced against the RELEASED surface, not cli main. cli 3835270 (#159 connection events) and 1780887 (#160 closed-session replay contract) were on main but unreleased at sync time and carry no doc-visible surface. daemon commits after 2a7cb15 (conn_log/audit) are internal."
    },
    {
      "date": "2026-07-17",
      "scope": "canonical `gateway` verb (gw = alias)",
      "released": {
        "clawee": "v0.1.92"
      },
      "repos": {
        "clawee-git/cli": "d253337"
      },
      "notes": "Reconstructed from web commit 413e5b9. docs/index.html only — llms-full.txt was missed and still carried `gw` as canonical until the 2026-07-29 sync."
    }
  ]
}
